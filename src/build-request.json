{
  "kind": "build_request",
  "title": "Enable purchase and memberships access paths",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-30",
      "text": "Update the main navigation so authenticated users can always navigate to the Predictions page, even when they do not yet have an active membership, and rely on the existing PremiumGate upsell to restrict access.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-4"
        ],
        "quotes": [
          "Enable purchase and memberships"
        ]
      },
      "acceptanceCriteria": [
        "When a user is signed in but does not have active access, the header (desktop and mobile) still shows a \"Predictions\" link.",
        "Clicking \"Predictions\" as a signed-in but non-premium user routes to the Predictions page and shows the existing subscribe call-to-action (from PremiumGate), rather than hiding the page via navigation.",
        "When a user has active access (or is admin), Predictions continues to behave as it does today (full access).",
        "All user-facing text remains in English."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui (read-only).",
    "Do not edit immutable frontend paths listed in SYSTEM_CONTEXT.",
    "Backend must remain a single Motoko actor in backend/main.mo; only add migration.mo if an actual state upgrade is required."
  ],
  "nonGoals": [
    "Do not add new membership plans beyond $25/month and $250/year.",
    "Do not add new authentication methods (Internet Identity remains the only login).",
    "Do not add any third-party payment providers beyond the existing Stripe flow."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}